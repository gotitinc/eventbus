#!/bin/sh
# replace with your key
export RELEASE_COOKIE="wU!;VAk/T|dNJ>KhG(sPE[KMj{wf8Jr=/{oCE_29"

export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=<%= @release.name %>@$(elixir -e ':inet.getif() |> elem(1) |> Enum.fetch!(0) |> (fn {{a, b, c, d}, _, _ } -> "#{a}.#{b}.#{c}.#{d}" end).() |> IO.puts')
